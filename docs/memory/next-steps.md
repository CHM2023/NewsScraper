# Next steps

Updated at the end of every step. Newest plan replaces the old one.

## Right now
1. Step 1 — project skeleton, `.gitignore`, `.env.example`, `CLAUDE.md`, memory
   bank. In progress.
2. Step 2 — `sql/001_init.sql` schema + `event_weights` seed.
3. Step 3 — `fetchers/calendar_skeleton.py` against FRED `releases/dates`.
