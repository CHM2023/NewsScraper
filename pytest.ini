[pytest]
# Import the project packages without installing them.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:common.*
    error::DeprecationWarning:db.*
    error::DeprecationWarning:fetchers.*
