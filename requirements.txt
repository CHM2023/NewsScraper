# Pinned for reproducible GitHub Actions runs. See docs/memory/decisions.md.
fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.5
# 2.11 validated the API key against a JWT-shaped regex and rejected Supabase's
# current sb_secret_ keys outright. 2.31 dropped that check. See decisions.md.
supabase==2.31.0
requests==2.32.3
pandas==2.2.3
python-dotenv==1.0.1
pytest==8.3.4
# 0.2.x broke against Yahoo's current API (YFTzMissingError on every ticker),
# which mattered because FRED no longer publishes a daily gold price at all.
yfinance==1.7.0
httpx==0.27.2
# Windows ships no IANA tz database; zoneinfo needs this to resolve
# America/New_York when converting release times to UTC.
tzdata==2024.2
# Postgres wire protocol, for applying sql/*.sql. PostgREST cannot run DDL
# and psql is not on the owner's PATH. Used only by scripts/apply_schema.py.
psycopg[binary]==3.2.3
